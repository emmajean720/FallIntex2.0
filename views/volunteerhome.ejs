<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Volunteer</title>
  <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
  <script>
    function toggleSewingQuestions(value) {
        const sewingQuestions = document.querySelector('.sewing-questions');
        if (value === "sewing") {
            sewingQuestions.style.display = 'block';
        } else {
            sewingQuestions.style.display = 'none';
        }
    }
</script>
<style>
  /* Style for the table element */
  table {
    width: 80%; /* Table takes up 80% of the page width */
    margin: 20px auto; /* Center the table horizontally */
    border-collapse: collapse; /* Collapse table borders for a cleaner look */
    font-size: 10px; /* Reduce the font size of the table */
  }
  /* Style for table header (th) and data cells (td) */
  th, td {
    border: 1px solid #ddd; /* Light gray border around each cell */
    padding: 5px; /* Reduce the padding to decrease row height */
    text-align: center; /* Center the text in the cells */
    line-height: 1.2; /* Adjust line height for smaller rows */
  }
  /* Style for table header (th) */
  th {
    background-color: #F2F2F2; /* Light gray background color for headers */
    font-weight: bold; /* Make header text bold */
  }
  /* Style for buttons */
  button {
    padding: 4px 8px; /* Adjust button size */
    margin: 2px; /* Small margin between buttons */
    font-size: 12px; /* Adjust button font size */
    cursor: pointer; /* Change cursor to pointer on hover */
  }
</style>
</head>
<body>
<!-- Nav Bar -->
<%- include("partials/navbar") %>
<!-- Main Content -->
<div class="main-content">
  <div class="card">
    <div class="card-header">New Upcoming Events</div>
    <div class="card-content">
        <p>New Upcoming events</p>
        <table>
          <thead>
            <tr>
              <!-- Table headers for each column -->
              <th>Organization</th>
              <th>Start Time</th>
              <th>End Time</th>
              <th>City</th>
              <th>Volunteers Needed</th>
              <th>Sign up</th>
            </tr>
          </thead>
          <tbody>
            <!-- Check if there are any event records -->
            <% if (event.length > 0) { %>
              <!-- Loop through each event record and display in a table row -->
              <% event.forEach(row => { %>
                <tr>
                  <!-- Hidden input fields for eventcode -->
                  <input type="hidden" value="event.eventcode">

                  <!-- Display event information-->
                  <td><%= row.organization %></td>
                  <td><%= row.eventstarttime %></td>
                  <td><%= row.eventstoptime %></td>
                  <td><%= row.eventcity %></td>
                  <td><%= row.statecode %></td>
                  <td><%= row.expectedparticipants%></td>
                  
                  <!-- Actions column with button to sign up -->
                  <td>
                    <!-- Edit button to edit Character details -->
                    <form action="/signupevent/<%= row.eventcode %>" method="GET" style="display:inline;">
                      <button type="submit">Sign-up</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            <% } else { %>
              <!-- If no character records, show a message indicating no data is available -->
              <tr>
                <td colspan="7">No data available</td> <!-- Span across all 7 columns -->
              </tr>
            <% } %>
          </tbody>
        </table>
        
        <!-- Button to add a new Pokémon record -->
        <form action="/addChar" method="GET" style="display: flex; justify-content: center; margin: 20px 0;">
          <button type="submit">Add Character</button> <!-- Submit button to go to the Add Character  page -->
        </form>
        <form action="/maintain" method="GET" style="display: flex; justify-content: center; margin: 20px 0;">
          <button type="submit">Maintain Planets</button> <!-- Submit button to go to the Add Character  page -->
        </form>
    </div>
  </div>
  <div class="card">
    <div class="card-header">Your Events</div>
    <div class="card-content">
        <p>Sign in here to view scheduled events, hours, and more!</p>
        <form action="/vlogin" method="POST">
            <label for="VLogin">Username:</label>
            <input type="text" id="VLogin" name="VLogin" required>
            <br>
            <label for="VPassword">Password:</label>
            <input type="password" id="VPassword" name="VPassword" required>
            <br>
            <button type="submit" style="margin-top: 20px;">Login</button>
        </form>
    </div>
  </div>
  <div class="card">
    <div class="card-header">Your Past Events</div>
    <div class="card-content">
        <p>Sign in here to view scheduled events, hours, and more!</p>
        <form action="/vlogin" method="POST">
            <label for="VLogin">Username:</label>
            <input type="text" id="VLogin" name="VLogin" required>
            <br>
            <label for="VPassword">Password:</label>
            <input type="password" id="VPassword" name="VPassword" required>
            <br>
            <button type="submit" style="margin-top: 20px;">Login</button>
        </form>
    </div>
  </div>
</div>
  <script>
    document.querySelectorAll('.card-header').forEach(header => {
      header.addEventListener('click', () => {
        const card = header.parentElement;
        card.classList.toggle('open');
      });
    });
    document.querySelector('form').addEventListener('submit', function(e) {
      const password = document.getElementById('password').value;
      const confirmPassword = document.getElementById('confirm-password').value;
      if (password !== confirmPassword) {
        e.preventDefault(); // Prevent form submission
        alert('Passwords do not match!');
      }
    });
  </script>
      <!-- Footer -->
      <%- include("partials/footer") %>
</body>
</html>